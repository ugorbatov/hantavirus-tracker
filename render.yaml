services:
  - type: web
    name: hantavirus-tracker
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEWS_API_KEY
        sync: false
